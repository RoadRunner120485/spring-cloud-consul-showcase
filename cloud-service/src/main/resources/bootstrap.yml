server:
  port: 8080

spring:
  application:
    name: cloud-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${service.registry.url:http://eureka:8080/eureka/}

---
spring:
  profiles: development

service:
  registry:
    url: http://localhost:8761/eureka/

---

spring:
  profiles: docker

server:
  port: 8080
